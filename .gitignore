# ── Dependencies ─────────────────────────────────────────────────────────────
node_modules/

# ── Environment - NEVER commit secrets ───────────────────────────────────────
.env
.env.*
!.env.example        # keep the template

# ── Runtime data - generated at runtime, user-specific ───────────────────────
data/
MEMORY.md
HEARTBEAT.md

# ── Skill embedding cache - auto-regenerated on startup ──────────────────────
# (included under data/ above, but listed explicitly for clarity)
# data/skill-embeddings.json

# ── Local-only dirs - never push to GitHub (files kept locally) ──────────────
/agents/
aegis-ui/            # web dashboard (separate repo)
components/          # scratch UI components
daemoreDir/          # scratch directory
docs/                # internal planning docs
PHASES.md            # internal phase planning
STATUS.md            # internal status tracking
DEEP_RESEARCH_REPORT.md
ANALYSIS.md
GIT_RULES.md         # local dev notes
.vscode/            # local VSCode settings
.PHASES.md           # internal phase planning
.FEATURES.md         # internal feature planning
FEATURES.md          # internal feature inventory
ROADMAP.md           # internal development roadmap

# ── Claude Code project context ───────────────────────────────────────────────
.claude/

# ── Test artifacts (not the test source files) ───────────────────────────────
tests/example.spec.ts      # Playwright example - not our tests
playwright.config.ts
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
/playwright/.auth/

# ── Playwright MCP scratch (downloads, traces, screenshots) ──────────────────
.playwright-mcp/

# ── Build artifacts ────────────────────────────────────────────────────────────
dist/
build/
*.log

# ── OS & editor ───────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
.vscode/settings.json
.idea/
*.swp
*.swo

# --------------- UI Package -----------------
daemora-ui/node_modules/
daemora-ui/dist/
daemora-ui/.vite/
daemora-ui/.env.local
daemora-ui/.env.*.local
daemora-ui/npm-debug.log*
daemora-ui/yarn-debug.log*
daemora-ui/yarn-error.log*
daemora-ui/pnpm-debug.log*

TASKS.md
/desktop/app/src-tauri/target/
/desktop/sidecar/daemora_sidecar/__pycache__/
/desktop/app/src-tauri/gen/

# Bundled runtime resources — regenerated by scripts/bundle-resources.sh
desktop/app/src-tauri/binaries/

# ── Credentials / secrets — never commit ──────────────────────────────────────
# Generic key + cert formats
*.key
*.pem
*.p12
*.pfx
*.crt
# Service-account / credential JSON files (GCP, Firebase, etc.)
*-key.json
*-credentials*.json
*-service-account*.json
*-sa.json
gcp-key.json
vertex-key.json
daemora-vertex-key.json
# Common secret directories
secrets/
.secrets/
private/
# Local-only / private convention files
*.local
*.private
# Raw email dumps
*.eml

# ── Runtime / data leakage (back-up to the existing data/ rule) ──────────────
*.sqlite
*.sqlite-*
*.db
*.db-*
*.db.backup-*
*.db.bak
*.tsbuildinfo
coverage/
.nyc_output/
.turbo/
.next/
.cache/
.parcel-cache/
tmp/
temp/
.tmp/
*.pid
*.seed
*.tgz

# ── Voice / audio scratch ────────────────────────────────────────────────────
*.recording.*
voice-debug-*/
*.wav.dump
*.pcm

# ── Editor / OS extras ───────────────────────────────────────────────────────
*~
.#*
\#*#
.AppleDouble
._*
.LSOverride
Icon?
.fleet/
.zed/
.cursor/

# ── Local scratch markdown ───────────────────────────────────────────────────
NOTES.md
SCRATCH.md
TODO.md
WIP.md
LOCAL.md
